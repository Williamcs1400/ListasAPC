#include <stdio.h>

int main(){
	double x, y, soma_x=0, soma_y=0, soma_x2=0, soma-y2=0, i=0, total=0;
	for(i=0; i<10; i++){
		scanf("%d %d", &x, &y);
		soma_x2+=();
		soma_x+=x;
		soma_y+=y;
		total+=(((10*soma_x)*y-(soma_x*soma_y))/)
	}
}